# agent-log

## 2026.08.30 16:11

已使用`pdftotext -layout`将641页参考教材转换为同目录下的TXT文件。转换结果共31,428行、约2.15MB，抽查标题页及正文文本可正常读取；后续查阅教材时应使用该TXT文件。

## 2026.08.30 16:20

教材Part III的乘法器内容可统一理解为三个阶段：部分积生成、部分积归约、最终二进制加法。其主要加速路线有两条：通过高基数和Booth重编码减少部分积数量；通过CSA、压缩树、阵列和流水线加快部分积归约并推迟全宽进位传播。

四章形成连续的设计空间：第9章建立逐位移位加法的低成本顺序基线；第10章用高基数、Modified Booth和CSA减少迭代次数及单轮延迟；第11章进一步空间展开为partial-tree、full-tree和array，并讨论符号、截断、规则性与流水化；第12章面向模块复用、串行接口、模运算、平方和乘加等具体约束重组上述方法。评价乘法器不能只看逻辑级数，还要同时考虑面积、布线、规则性、延迟、吞吐和功耗。

## 2026.08.30 18:53

对于补码逐位右移乘法器，可将控制关系概括为：当前乘数位`xi`决定本轮加入`0`还是被乘数`a`；最后一轮标志`l`决定非零部分积采用`+a`还是`-a`。若用`~a+1`实现减法，则仅当`l=1`且`xi=1`时选择`~a`并令`cin=1`；当`xi=0`时应加入0，`l`不产生数值作用。

## 2026.08.30 19:09

普通k位加法若只保留模2^k的低k位，有符号与无符号可使用同一加法电路；但逐位乘法器需要保留加法后、右移前的第k+1位，此时必须区分数值解释。无符号操作数应零扩展，补码操作数应符号扩展，不能统一把k位加法器的`cout`当作补码结果的新增高位。图9.8采用直接方案：先将k位补码操作数符号扩展为k+1位，再用标准k+1位加法器运算；也可使用k位加法器配合专门的高位逻辑实现同一效果。

## 2026.08.30 22:05

Booth重编码可理解为边界编码：令`x[-1]=0`并取`y[i]=x[i-1]-x[i]`，连续1内部的相邻位相同，编码为0，只有低端边界产生-1、高端外侧边界产生+1。对于固定k位补码乘数，最高端不额外补0，因而最高处未闭合的1区间自然保留为负权项；这使Booth无需单独判断乘数符号即可处理补码有符号乘法。无符号数若最高位为1，则需在左侧额外补0以生成闭合边界。

## 2026.08.30 22:11

Radix-2 Booth在固定周期现代硬件中通常没有稳定的效率优势：它主要改变每轮执行加、减或空操作，轮数仍为k，而且同步数据通路往往仍经过加法器。其主要价值是建立带符号数字和边界重编码的概念。Radix-4 Modified Booth把数字集合扩展为`-2、-1、0、1、2`并一次处理两个乘数位，才可将部分积数及顺序迭代周期稳定地降至约一半。

## 2026.08.30 22:27

Radix-4 Modified Booth采用重叠三位窗口：第j个编码数字由`x[2j+1:2j-1]`决定，相邻窗口共享边界位`x[2j-1]`，编码值为`z[j]=-2*x[2j+1]+x[2j]+x[2j-1]`。最低端始终令`x[-1]=0`；最高端无符号数采用零扩展，以闭合最高端的1区间，必要时产生额外编码位；补码数采用符号扩展，保留最高端负权，因此偶数字长直接省略无符号编码产生的额外位，奇数字长令`x[k]=x[k-1]`。边界处理先于机械的两位分组，是正确理解编码位数的关键。

## 2026.08.30 23:05

第10.3节区分CSA的两类作用：CSA可把更多输入压缩为两行，从而容纳Radix-4所需的多个部分积，但若随后每轮仍经CPA，则只减少轮数而不缩短单轮延迟；更重要的做法是让累计部分积长期保持`sum+carry`的stored-carry冗余形式，使前面的迭代只经过选择器和CSA，延迟基本不随字长增长，最后才执行一次CPA。再与Modified Booth结合，可同时把周期数约减半并缩短每周期延迟。已确定的低位乘积必须从冗余表示局部转换成普通二进制后移入寄存器低半部；负Booth部分积的补码`+1`可作为对应列的额外bit直接送入CSA。

## 2026.08.30 23:36

第10.6节以k表示字长、b表示Radix-2^b乘法器每轮处理的二进制位数，建立面积与时间模型：并行生成并归约b个部分积需要`A=O(bk)`面积；约k/b轮、每轮O(log b)的CSA树，加一次O(log k)最终CPA，得到`T=O((k/b)log b+log k)`。增大b本质上是近似线性增加面积以减少轮数。按AT成本效率衡量，低基数顺序乘法器在渐近上可能优于高基数或全树结构，因此大量独立运算有时更适合部署更多慢单元而非少量快单元；但渐近结论不等于实际字长下的设计结论，常见有限k中高基数和树形乘法器仍很实用，最终选择还取决于延迟、吞吐、面积及具体工艺。

## 2026.08.31 15:54

“RCA乘法器”与“CSA乘法器”不是本书最精确的分类：RCA只是CPA的一种实现，而乘法器通常应分别说明部分积归约结构和最终CPA。逐行串接、错位对齐的RCA部分积阵列不能粗略按k个完整k位RCA相继稳定来估算为O(k²)延迟；由于低位结果逐步产生，依赖波前的最长路径通常只跨越O(k)个FA，常可看成约一条纵向级链加一条横向进位链。图11.11同样是单边CSA链加最终RCA，延迟也是O(k)且约由两段线性路径构成。两者可有相近渐近延迟，但不能据此推广到任意串联CPA，也不能与O(log k)深度的平衡Wallace/Dadda CSA树混淆；实际常数、布线和流水能力仍有差别。

## 2026.08.31 23:09

Wallace Tree和Dadda Tree可从各权重列的bit数量，即列高度来理解。一个FA从本列取3个bit，向本列返回1个sum并向左侧高一权重列产生1个carry，因此净效果为“本列-2、左列+1”，同时把全局bit总数从3减为2；一个HA从本列取2个bit，返回1个sum并向左列产生1个carry，净效果为“本列-1、左列+1”，但全局bit总数仍为2。故FA真正完成压缩，HA主要将高度从当前列转移到高权列。Wallace与Dadda使用相同的列变换，区别在于每层的安排策略：Wallace尽早、尽可能压缩，Dadda只压到预定目标高度，以保持树深的同时减少FA和HA。

## 2026.09.01 00:03

进一步确认：仅给定列高序列不能唯一确定所谓Wallace Tree，因为文献中存在按三行分组、逐列全压缩、仅FA优先并按需使用HA等不同合法调度。要得到绝对可执行的结果，必须先声明约定，并严格规定同一stage产生的sum和carry只能进入下一stage，不能被本stage再次消费。统一列高关系为`h_next[i]=h[i]-2*FA[i]-HA[i]+FA[i-1]+HA[i-1]`，其中`i-1`为低一权重列。

建议采用无歧义规范：列式Wallace每层对本stage输入的每列放`floor(h[i]/3)`个FA，并明确选择“余2点放HA”或“余2点直通”，前者是完全贪心版本；Dadda先生成目标高度`2,3,4,6,9,...`并逆序逐层处理，每层从低权到高权扫描，用低权列carry做下一层高度预算，超目标2点优先用FA，剩余超目标1点用HA。教材中的具体Wallace图包含缩短最终CPA等额外HA调度，属于合法变体，但不能视为仅由列高推出的唯一标准答案。

## 2026.09.01 00:12

方向约定纠正：后续列高序列统一按教材图的视觉顺序书写，即左侧为高权、右侧为低权，FA／HA的carry向左侧相邻列移动。不得再将“数组下标从低权递增”的公式与“高权在左”的打印序列混用。按此约定，`1 2 3 4 3 2 1`经`XX HA FA FA FA HA XX`归约后是`2 2 2 3 2 1 1`；此前写成`1 1 2 3 2 2 2`是将同一结果反向输出造成的错误。

## 2026.09.01 00:29

纠正此前推荐的“余2点一律用HA”规则：该规则会在最高权列无意义地产生新列，不是合理的Wallace综合准则。教材式Wallace设计应分开两个目标：每层对所有可用三元组立即放FA，以取得最少归约层数；HA只用于两种情况，一是防止低权列送来的下一层carry使本列超过该层目标高度，二是在最低权的两行重叠边界放置HA，使该列提前成为确定结果位并缩短最终CPA。不得仅因某列余2点就在最高权侧放HA。

对于标准部分积矩阵，可执行流程为：当前最大高度H对应下一目标高度`ceil(2H/3)`；先按当前stage输入为每列安排`floor(h/3)`个FA；再从低权向高权计算下一层高度，在最低权重叠边界可选用一个HA以缩短CPA，并在任何下一层高度超过目标的位置用HA修正；所有本层输出只进入下一stage。该流程复现教材4位Wallace例子的两层选择，同时避免最高权无效扩列。Wallace定义的是最早归约策略，若不声明最终CPA宽度等次级目标，HA位置本来就不唯一。

## 2026.09.01 15:18

Wallace的可执行规则：

1. `T=ceil(2H/3)`，其中H为本层的最大高度。
2. 对于每一列放置`floor(h/3)`个FA。
3. 对于权重最低的`h=2`列放置1个HA缩短CPA。
4. 预计算更新后的层高，从低向高扫描，对于任意`h>T`的放置1个HA修正，更新层高。

## 2026.09.01 15:46

Dadda的可执行流程：

1. 生成目标高度序列：`n(0)=2`，`n(h+1)=floor(3*n(h)/2)`，即`2,3,4,6,9,13,19,28,42,...`。
2. 设初始点阵的最大列高为H，令首层目标`T=n(h)`，其中`n(h)`取满足`n(h)<H`的最大值；后续各层目标依次取`n(h-1),n(h-2),...,2`，不再根据更新后的最大列高重新选择。
3. 对每个目标T，从低权向高权逐列扫描。令当前有效高度h等于本列原有高度加低权列在本层产生、将进入本列的carry数量，并令`excess=max(0,h-T)`。
4. 对该列放置`floor(excess/2)`个FA和`excess mod 2`个HA，使下一层本列高度不超过T；FA和HA只能消费本stage当前列原有的bit，其输出进入下一stage。
5. 按目标序列重复归约，直至所有列高度不超过2，再使用最终CPA。

## 2026.09.01 21:10

对于同宽`k×k`二补码乘法并保留完整`2k`位结果，Modified Baugh-Wooley可概括为：在所有部分积`a[i]x[j]`中，将恰好包含一个符号位，即`i=k-1,j<k-1`或`i<k-1,j=k-1`的项取反；普通位之间的部分积和同时包含两个符号位的`a[k-1]x[k-1]`保持不变；最后加入一个`2k`位修正常数，其第`2k-1`位和第k位为1，其余位为0。转换后的点阵仅含正权bit，可交给阵列或树状归约器。

## 2026.09.01 21:18

在图11.12式CSA阵列中，Modified Baugh-Wooley的两个修正bit无需增加完整CSA行：第k位恰好是最终CPA最低位及`p[k]`的产生位置，可利用该处原本为0的低位进位／空闲输入植入常数1；第`2k-1`位位于最高权边界，只需与该处已有bit合并，通常增加一个HA即可。因此有符号支持主要改变边界单元，开销很小。

## 2026.09.06 09:55

图13.5中`(k+1)`位试探量减`k`位除数可只用一个k位减法器。设当前`2k`位部分余数寄存器为R，并从其旧值构造左移后的组合下一状态：`b=R[2k-1]`是左移移出的最高位，`A=R[2k-2:k-1]`是左移后与除数d对齐的k位高部，完整待比较量为`U={b,A}=b*2^k+A`；低部由`R[k-2:0]`直接移至下一状态的`[k-1:1]`。现代单沿RTL不必先把R实际左移，也不需要专用FF保存b；在时钟沿前，旧R稳定，b可直接接商位判断逻辑，组合电路算完后在同一时钟沿统一写入R_next。教材中的专用FF只对应“周期开始先实际移位，再进行组合计算”的微结构描述，不是算法必需。

k位减法器用二补码加法计算`A-d=A+~d+1`。其低k位始终是`(A-d) mod 2^k`，而`cout=1`表示`A>=d`、没有借位，`cout=0`表示`A<d`、减法需要从第k位借1。若`b=1,cout=0`，外部高位b恰好提供这次借位，因此减法器的低k位结果`2^k+A-d`正是完整结果`U-d`；若`b=0,cout=0`则完整U也不够减。故合法迭代中的商位判断为`q=b OR cout`：`b=0,cout=0`时不减并选择A；其余有效情况均减并选择减法器结果。合法部分余数范围保证`b=1,cout=1`不会出现，否则减后结果仍需k+1位。

## 2026.09.06 10:01

恢复式有符号除法适合采用间接处理：先保存输入符号并对`|z|`和`|d|`执行无符号除法，得到幅值`|q|`和`|s|`，再按需对结果取二补码。其符号规则为`sign(q)=sign(z) XOR sign(d)`和`sign(s)=sign(z)`；即商在被除数与除数异号时为负，余数始终与被除数同号，从而满足`z=d*q+s`和`|s|<|d|`。该规则对应商向0截断的整数除法。

## 2026.09.06 16:05

对于适用于RV32M的统一非恢复除法数据通路，`DIV`模式将部分余数`s`初始化为被除数`z`的32位有符号扩展；`DIVU`模式将`s`初始化为`z`的32位无符号扩展。除数符号`sign(d)`只在有符号模式下有效：`DIV`中用`sign(s)`与`sign(d)`共同决定本轮选择`d`还是`-d`，`DIVU`中将`sign(d)`固定视为0，不参与有符号控制。除零及`INT_MIN/-1`等ISA特殊情况仍由外部控制逻辑单独处理。

## 2026.09.06 18:49

必须区分非恢复的数据通路、有符号支持和商的外部编码。非恢复数据通路每轮根据旧部分余数选择加d或减d；在普通无符号Non-Restoring中，标准二进制商位`0/1`由运算后部分余数是否非负决定，可由`cout`直接得到。也可以从代数上把“减d”视为商数字`+1`、“加d”视为商数字`-1`，从而把同一串操作解释为BSD冗余商。教材13.4选择以该`{-1,+1}`递推来推导直接有符号除法，因此算法说明中两者被绑定，但硬件不必存储BSD商，可边迭代边转换成标准二补码。故“支持有符号输入”和“商寄存器采用BSD还是标准编码”是不同的设计问题；不过直接有符号模式的标准商位不能简单照搬正除数无符号模式下的`q=cout`，仍须实现与教材递推等价的在线转换及最终修正。

## 2026.09.06 22:42

重读教材第13.1、13.3和13.4节后确认：二进制逐次除法的统一递推是`s(j)=2s(j-1)-q(k-j)*(2^k*d)`，部分余数左移后，从高位到低位产生商位。恢复式每轮假设商位为1并试减；只有试减结果非负时保存该差值并置商位1，否则丢弃差值、恢复为左移前的值并置商位0。图13.5的无符号商位判断是“左移移出的MSB为1或试减加法器`cout=1`”，即`q=MSB(2s) OR cout`，不能简化为始终使用`cout`。

非恢复式每轮始终保存试算结果，不在本轮恢复：若当前部分余数为负，下一轮改为加除数；这样通过“负值保留、下一轮补偿”得到与恢复式相同的有效递推，并避免一个周期内依次完成移位、加法器传播和商位采样所造成的时序压力。无符号非恢复结束时若最终余数为负，仍需最终校正。

教材还将非恢复每轮操作解释为BSD冗余商数字：减法对应`+1`，加法对应`-1`。直接有符号除法据此选择操作：若部分余数符号与除数符号相同则减除数并取`+1`，否则加除数并取`-1`。结束时需要两类处理：把`{-1,+1}`商转换成标准二补码，以及当最终余数符号与被除数符号不同时进行余数和商的联合校正。硬件实现中，数据通路、输入有符号支持和商寄存器采用BSD或标准编码是三个相互独立的设计问题。

## 2026.09.06 23:29

在`Divider.md`当前Nonrestoring推演约定中，左移后括号中的`MSB`是控制信号而不是参与数值运算的额外位：`MSB=0`选择`SUB`，`MSB=1`选择`ADD`。例如`1000_0000/1111`第一轮左移得到`(1)0000_0000`，应执行4-bit的`0000+1111=1111`，`cout=0`，故`q3=0`；该`1`既不送入4-bit加法器，也不写回下一状态。4-bit加法器的`cout`只用于商位决策，低4位结果与左移后的低4位共同形成下一状态。此前将左移MSB解释成完整试减量并据此判断首位商，是错误的。

## 2026.09.12 14:44

SRT除法器与PD图调研第一步：精读教材13.1~14.6，按演进脉络整理各部分要点，作为后续统一梳理（含改用整数讲解）的基础。

### 13.1~13.3 整数与分数的对应关系（消解分数困惑的钥匙）

- 基本除法方程`z=d×q+s`配合`sign(s)=sign(z)`与`|s|<|d|`唯一确定结果。整数情形z是2k位、d是k位、q是k位，溢出检查为`z<2^k·d`（即z高k位小于d）。
- 整数递推：`s(j)=2s(j-1)-q[k-j]·(2^k·d)`，`s(0)=z`，`s(k)=2^k·s`。其中`2^k·d`是把除数对齐到部分余数高k位的"对齐除数"，第一次试减时除数与部分余数高k位对齐，低k位不受影响。
- 分数递推：`s_frac(j)=2s_frac(j-1)-q[-j]·d_frac`，`s_frac(0)=z_frac`，`s_frac(k)=2^k·s_frac`。
- 整数与分数严格等价：把整数式两边同乘`2^-2k`得`z_frac=d_frac×q_frac+2^-k·s_frac`，即同一过程两种写法，唯一差别是最终余数在分数写法里要右移k位（补k个前导0）。教材13.1明确说"分数除法可以重述为整数除法，反之亦然"。
- 关键映射：分数写法中`d_frac∈[1/2,1)`（规范化为"0.1xxx"）等价于整数写法中d的MSB=1（`2^(k-1)≤d<2^k`）。SRT里所有"1/2"阈值本质上都是相应满量程的一半——部分余数满量程`2^(2k)`的一半是`2^(2k-1)`，除数满量程`2^k`的一半是`2^(k-1)`。这是后续改整数讲解的抓手。

### 13.4 非恢复除法与有符号除法（{-1,1}的BSD商）

- 非恢复：每轮假设`q=1`做减法，但无论正负都把差写入部分余数（"不恢复"）。若上轮减过头变负，本轮改加即可补偿：`2(u-2^k·d)+2^k·d=2u-2^k·d`，正好等于恢复后再试减的结果。
- 商数字集`{1,-1}`：减对应+1、加对应-1，目标是把部分余数符号拉到与被除数一致。
- 有符号直接除法商位规则：`sign(s)=sign(d)`则`q=1`，否则`q=-1`。
- 结束需两件事：①把`{1,-1}`的BSD商转二补码（可在线转换）；②若最终余数符号与被除数不同，做余数与商的联合校正（`{1,-1}`只能表示奇数，商为偶数必校正）。
- 商转换：把-1替换为0得到p（满足`q_i=2p_i-1`），再把p最高位取反、整体左移1位、LSB填1，得`q=(¬p[k-1] p[k-2]...p[0] 1)`。正确性由`Σ(2p_i-1)2^i`展开验证。
- 在线转换不溢出的条件：`p[k-1]=p[k-2]`，即`q[k-1]≠q[k-2]`，等价于`sign(z)≠sign(s(1))`。

### 13.6 Radix-2 SRT（引入0、把阈值从±d变成常数±1/2）

- 动机：非恢复每轮都做加减，但当`2s∈[-d,d)`时加减会改变符号，其实可选`q=0`只移位（"跳0"）。问题是如何不试减就知道`2s`是否在`[-d,d)`内——试减比省下的还贵。
- SRT解法（Sweeney、Robertson、Tocher独立提出）：假设`d≥1/2`（规范化），把部分余数限制到`[-1/2,1/2)`而非`[-d,d)`，于是判断阈值变成常数`±1/2`而非`±d`。
- 若初始z不在`[-1/2,1/2)`，先右移1位，最后把商和余数加倍补偿（共k+1轮）。
- 商位规则：`2s<-1/2`→`q=-1`；`2s≥1/2`→`q=+1`；否则`q=0`。
- 判断只需看`2s`的两位：`2s≥1/2`即`2s=0.1xx`（`u0·u-1`），`2s<-1/2`即`2s=1.0xx`（`ū0·ū-1`），两个二输入AND门。
- 本质与Booth重编码同构：把"一次加减+一串反向加减"合并成"一串空操作+一次加减"，利用`±(2^j-2^(j-1)-...-2^i)=±2^i`。
- 可变移位版：跳过连续相同前导位，平均移位2.67位；但在同步+CSA设计中已弃用，因为判断是否跳过的逻辑延迟与CSA加法本身相当。
- 商含0后，13.4的转换算法（仅对`{1,-1}`成立）不适用，需改用在线转换[Erce87]或两个商寄存器分别装正负数字再相减。
- 章末预览快除法两方向：减少被加数个数（高基数）、更快相加（CSA存余数）。除法比乘法难在：被减的项不能预先知道，依赖逐轮产生的商位，而商位又依赖部分余数与除数的大小关系。

### 14.1 高基数基础（引入商冗余是加速的关键）

- 基数r递推：`s(j)=r·s(j-1)-q[k-j]·(r^k·d)`，`s(0)=z`，`s(k)=r^k·s`，`r=2^b`一轮产b位商。
- 两大难点：①高基数下猜商更难（radix-4要试减d和2d，几乎抵消加速）；②被减项逐轮确定且商位依赖部分余数，CSA存余数下符号/大小不易得。
- 核心思想：引入商的冗余表示。非冗余商(0/1)没有容错空间，最高位猜错无法挽回；若允许`{-1,1}`，猜1错了下一轮可改写成`(1 -1...)`。冗余越大，容错余量越大，判定商位所需检查的部分余数位数越少。

### 14.2 使用CSA（重叠区让近似比较成为可能，p-d图登场）

- 部分余数存成sum+carry，精确比较要完整进位传播；但冗余商带来的重叠区允许近似比较。
- 图14.3用数字集`{-1,0,1}`、部分余数范围`[-d,d)`，阈值取常数`-1/2`与`0`：`2s∈[-2d,-1/2)`选-1、`[-1/2,0)`选0、`[0,2d)`选1。阈值落在重叠区内部，故无需精确比较。
- 判定算法：取存余数两分量u、v的最高4位相加得`t=u[-2,1]+v[-2,1]`；`t<-1/2`→-1、`t≥0`→1、否则0。正确性来自：每个分量截断误差<1/4（截断只减不增，总误差<1/2），因此`t<-1/2`保证真值`2s<0`、`t≥0`保证真值`2s≥0`、`-1/2≤t<0`保证真值落在`[-d,d)`内。
- 图14.4框图：`Select q-j`盒（4位快加法器+逻辑/16项表/256×2表/8输入PLA）→mux选`0/d/d_compl`→CSA→最后CPA。每轮只付查表+CSA的少量门延迟。
- 为何13.6的SRT不能直接用存余数：图14.5显示其重叠区宽度为`1-d`，d→1时→0，没有误差余量。而图14.3版本（范围`[-d,d)`）重叠区宽度为d≥1/2，有稳定余量，故14.2改用它。
- p-d图（p=移位后部分余数，d=除数）引入：图14.6对应radix-2、`q∈[-1,1]`、`d∈[1/2,1)`、部分余数`[-d,d)`。`p=-d`与`p=d`之间是0的合法区。决策线`p=0`与`p=-1/2`是水平线（与d无关），误差余量1/2平分给两分量各1/4。
- 逻辑式：`Non0=t1∨t0∨t-1=¬(t1t0t-1)`，`Sign=t1(t0∨t-1)`。
- 关于d轴不对称：2's补码截断对正负值影响不对称（截断总是减小数值），负侧需要-1/2的偏移，故决策线不对称。有符号除数时p-d图要扩展到负d的四个象限。

### 14.3 Radix-4 SRT（数字集[-3,3]→[-2,2]、h=2/3）

- 先看`q∈[-3,3]`：首次出现非水平决策边界——选3还是2不仅看p还看d的1位`d-2`（判断`d∈[1/2,3/4)`还是`[3/4,1)`）。
- ±3需预存3d（类比radix-4乘法的3a），用Booth思路把数字集限制为`[-2,2]`以避开3d。
- 限制`[-2,2]`须缩小部分余数范围：设`[-hd,hd)`，要能被±2d拉回，需`4hd-2d≤hd`→`h≤2/3`，取`h=2/3`。
- 图14.10：重叠区变窄，需更高精度；阶梯边界意味着要检查p的5位（含符号）与d的4位（`d-1`也提供符号信息）。
- 硬件相对图14.4的变化：d的4位送入选择盒、四输入mux（`d、2d`及其补）、商从radix-4有符号数字集`[-2,2]`转二补码更复杂。
- 这是Intel Pentium采用的算法。

### 14.4 通用高基数（h=α/(r-1)）

- radix-r、数字集`[-α,α]`（`α<r-1`）：部分余数范围`[-hd,hd)`，`h=α/(r-1)`。radix-8用`[-4,4]`、范围`[-4d/7,4d/7)`；3d可预存或由CSA树动态产生（`2d+d`）。
- 更大冗余数字集如`[-7,7]`：重叠区更宽、需更少精度，但比较更多、要生成难倍数(±5,±7)。
- 图14.11通用radix-r框图。商用长期停在radix-4，Penryn宣称用radix-16。

### 14.5 商位选择（不确定矩形与平铺）

- 不确定矩形：查p的4位、d的3位定位到点A，截断使真值落在A处的矩形内；只要整块矩形落在某数字区域内即可安全选择。点B（p的3位、d的4位）矩形两倍高一半宽，横跨两个数字区域，无法唯一确定。
- 目标：最大化不确定矩形（即最小化需检查位数）。用给定尺寸矩形从原点平铺整个p-d图，验证没有tile同时穿过重叠区两条边界（可触碰一条）；等价于能在每个重叠区内嵌入沿tile边界的阶梯路径。
- 平铺成功后给每个tile分配数字值（左下角点对应的表项）；有选择时（暗tile）按实现/工艺标准定。
- p为CSA形式时，要得到p的l位精度需检查两分量各`l+1`位，故要权衡（例：B点12位优于A点13位）。
- Pentium bug：p-d图数值生成后，脚本把表项下载到PLA时误删5项，命中时读出0而非+2；这些项极少被访问故测试未发现。教训：完全落在不可行区的tile可删，部分落在可行区的绝不能删。Coe/Tang：除数以`xxxxx111111`开头才可能出错。

### 14.6 p-d图实践（精度下界与不对称）

- 目标：找最粗网格使阶梯边界落在重叠区内。无闭式公式，需试错，用解析结果限搜索空间。
- 用α与α-1的重叠区（最窄）求界：`Δd=d_min·(2h-1)/(-h+α)`、`Δp=d_min·(2h-1)`。radix-4`[-2,2]`：`Δd=1/8`、`Δp=1/6`→至少d的3位与p的3位；实际要p的3位与d的4(3)位；p为CSA形式则每分量4位。
- 不对称：2's补码截断对正负不对称，不能用"取反"复用表项，必须覆盖四象限，符号信息加1位。
- 暗tile有选择：ROM实现无成本差别，PLA实现中构成部分don't-care可化简，化简程度依赖商数字编码。
- 实践中可选"几乎够好"的低精度，少数越界矩形作为例外用更多输入处理（图14.17的4个星号tile），可省1位。
- 结论：高基数除法比高基数乘法更难，需形式验证与大量测试。

### 本步形成的演进主线（待后续与用户逐步确认并整数化）

`{1,-1}`非恢复(13.4)→`{1,0,-1}`+规范化到[-1/2,1/2)使阈值为常数(13.6 SRT)→用范围[-d,d)保持重叠区宽d以获得稳定容错、从而能用CSA近似比较(14.2)→高基数r+受限数字集[-α,α]与h=α/(r-1)(14.1/14.3/14.4)→p-d图与不确定矩形求精度、平铺与查表(14.5/14.6)。

待讨论点：①`[-d,d]`的确切含义（部分余数不变式，p-d图p轴的合法范围）；②如何把13.6/14.2的分数例子（如`.01000101/.1010`）改写成整数例子（对应`69/10=6余9`，对齐除数160、阈值128）；③商冗余为何是加速关键的最直观解释。

## 2026.09.12 16:52

13.4完整梳理：引入`{-1,1}`BSD商如何自然支持有符号除法、如何调整原有非恢复的商生成、以及所需的收尾。

### 换视角：商位从"结果的符号"改为"所执行的操作"

此前`Divider.md`的无符号非恢复用`q=cout`定商位：先由`s`的符号决定加/减，再看运算**结果的符号**给`q`取值，得到标准二进制`{0,1}`。这套对有符号不能直接套用，因为负数商无法用纯`0/1`逐位表达出符号。

教材13.4改一个角度：非恢复每轮要么减、要么加，从不空转，于是把"做减法"记为商数字`+1`、"做加法"记为`-1`，商变成一串`{-1,1}`的BSD数字。递推统一为`s(j)=2s(j-1)-q[k-j]·(2^k·d)`，`q=+1`减`2^k·d`、`q=-1`加`2^k·d`。

关键调整：BSD方法里商位不再需要看运算结果的符号，**操作是什么、商位就是什么**。

### 为什么能支持有符号：统一的选操作规则

选操作规则可写成对所有符号统一的形式：`sign(s)=sign(d)`→`q=+1`（减），`sign(s)≠sign(d)`→`q=-1`（加）。它的效果始终是"把部分余数朝0拉"：同号则减`2^k·d`、异号则加`2^k·d`，都能使`|s|`变小，与z、d正负无关。无符号情形只是`d>0`（`sign(d)=+`）的特例，该规则退化为"`s`为正减、为负加"。

符号信息藏在`±1`数字本身里：值为负的商由一串`-1`表达，不需要额外符号寄存器；BSD商天然有符号，转补码时符号自动归位。

### 收尾一：BSD商转二补码

k位BSD商`q=(q[k-1]···q[0])`转换两步：①把每个`-1`换成`0`得`p`，满足`q_i=2p_i-1`；②`p[k-1]`取反、整体左移1位、LSB填1，得`(¬p[k-1] p[k-2]···p[0] 1)`。正确性由`Σ(2p_i-1)2^i=Σq_i·2^i=q`验证。

在线转换：第1轮由`q[k-1]`定符号位`¬p[k-1]`，此后每轮"减"移入1、"加"移入0，最后一轮额外补LSB的1。

不溢出条件：商能装进k位补码需`¬p[k-1]=p[k-2]`（符号扩展），即`p[k-1]≠p[k-2]`、`q[k-1]≠q[k-2]`，等价于`sign(z)≠sign(s(1))`。教材该处写`p[k-1]=p[k-2]`为笔误，正确为"不等"。

### 收尾二：最终校正（商与余数联合）

当`s(k)`符号与`z`不同时校正：若`sign(s(k))=sign(d)`则`s←s-2^k·d、q←q+1`；否则`s←s+2^k·d、q←q-1`。

为何需要：`{-1,1}`的k位BSD商只能表示奇数（末位`±1`决定奇偶），故真商为偶数时BSD商必差1；同时最后一下加/减冲过头越过0，余数符号反了。

为何按`sign(d)`定方向：校正是补"最后一步"，把余数从0的错误一侧翻到对齐`z`的一侧；因每步操作量是`±2^k·d`，`sign(s(k))`与`sign(d)`相同/相反决定该往哪边翻。校正后恒有`sign(s)=sign(z)`、`|s|<d`，且`z=d·q+s`保持。

### 整数例子

- 117/10=11余7（商为奇数，无需校正）：BSD商`(1 1 -1 1)`→`01011=11`，`s(4)=112`为正与z同号，不校正。
- 45/7=6余3（商为偶数，需校正）：BSD商`(1 -1 1 1)`→`00111=7`，`s(4)=-64`为负，因`sign(s(4))≠sign(d)`加`2^4·d`得`48=16×3`（余3）且商减1得6。
- -45/7=-6余-3（有符号）：BSD商`(-1 1 -1 -1)`→`11001=-7`，`s(4)=64`为正，因`sign(s(4))=sign(d)`减`2^4·d`得`-48=16×(-3)`（余-3）且商加1得-6。

45/7与-45/7的BSD商序列相同（都是"减加减减"）、校正方向也相同，仅`d`符号使实际加减方向与余数符号不同——正是"操作即商位、符号藏在数字里"的体现。

### 与`q=cout`方法的关系

`q=cout`方法商位是结果符号，最后一次`cout=0`已把`q0=0`定对，故校正只动余数不动商；BSD方法商位是操作（`q0=+1`），最后一下"减"冲过头，商差1、余数符号反，两者须一起校正。两者是同一算法的两种记账方式，最终结果一致；BSD方式把符号并入数字，才使13.4能一步扩展到有符号直接除法，并为13.6引入`0`、14章引入冗余容错铺路。

## 2026.09.12 18:10

13.4溢出检查：无符号/有符号的z、d公式，以及迭代中免额外硬件的前两位检查。

### 先验检查公式（仅由z、d决定）

- 无符号：`z < 2^k·d`，即`z >> k < d`（z的高k位<d）。商`q∈[0,2^k)`，k位全为幅值。
- 有符号：`|z| < 2^(k-1)·|d|`，即`|z| >> (k-1) < |d|`。商`q∈[-2^(k-1),2^(k-1)-1]`，最高位被符号占用，只剩`k-1`位幅值。

有符号比无符号有两点变化：多取一次绝对值（符号不参与溢出判断），且除数对齐少移一位（`2^k`→`2^(k-1)`）。两者合起来是范围恰好缩一半：k位补码商只有`k-1`位幅值，故除数对齐尺度从`2^k`降到`2^(k-1)`。例：k=4、d=10时无符号`z<160`（商最大15），有符号`|z|<80`（|q|最大7）。

该先验检查等价于教材的在线判断`sign(z)≠sign(s(1))`（不溢出）：以d>0为例，z>0时`q[k-1]=+1、s(1)=2z-2^k·d`，不溢出⟺`s(1)<0`⟺`z<2^(k-1)d`；z<0时`q[k-1]=-1、s(1)=2z+2^k·d`，不溢出⟺`s(1)>0`⟺`|z|<2^(k-1)d`。合起来即`|z|<2^(k-1)|d|`。

边界：该检查把`q=-2^(k-1)`（如-16/2=-8）也判为溢出，因为BSD`{-1,1}`到不了它（未校正的BSD商需先经-9或-7，而它们要k+1位才表示）。这是有符号除法经典边界（类比INT_MIN/-1），实际实现需特殊处理。

### 迭代中检查（免额外硬件的做法，教材实际采用）

不必在开头做幅值/移位/比较，而是让溢出在前两轮"自己冒出来"：看最先产生的两个商位是否同号。

- `q[k-1]=q[k-2]`（前两位同号）→ 溢出；
- `q[k-1]≠q[k-2]`（前两位异号）→ 不溢出。

p同理（`q_i=2p_i-1`，q相同⟺p相同）：`p[k-1]=p[k-2]`→溢出。

直觉：前两位同号（`1 1···`或`-1 -1···`）把幅值推到至少`2^(k-1)`，例`(1 1 -1 1)=11≥8`溢出、`(-1 -1 1 1)=-9`溢出；前两位异号（`1 -1···`或`-1 1···`）第二位反号抵消，幅值最多`2^(k-1)-1`，例`(1 -1 1 1)=7`、`(-1 1 -1 -1)=-7`均不溢出。

与`sign(z)≠sign(s(1))`的等价：`q[k-1]`由`sign(z)`对`sign(d)`决定、`q[k-2]`由`sign(s(1))`对`sign(d)`决定，sign(d)为常数，故`q[k-1]=q[k-2]`⟺`sign(z)=sign(s(1))`。

为何更便宜：`q[k-1]`、`q[k-2]`本就是第1、2轮算出的商位（副产物），比较只需一次1位异或；而先验检查需先求`|z|`、`|d|`（条件取反）、`|d|`移位、再比较，且在开头关键路径上。触发时机：第1轮末`sign(s(1))`可得即可比`sign(z)`，或第2轮末`q[k-2]`产生后直接比`q[k-1]`。这是13.4用在线转换而非先验检查的原因。

## 2026.09.12 18:17

13.4共识版总结（硬件视角）：BSD商不是简单把0/1换成-1/1，而是先改判商逻辑、后简化转换、再补最后校正。

### 判商逻辑：合并为一次判断

- 原无符号非恢复：先由`s`的符号定"加/减"，再由运算**结果的符号**（`q=cout`）定商位0/1，是两次判断；
- BSD：`sign(s)=sign(d)`则减、否则加，而**商位就是操作本身**（减=+1、加=-1），一次判断。

关键不是把0/1换成-1/1，而是把"操作"与"商位"统一成同一个符号，符号信息才并入数字、天然支持有符号。

### 转换简化：根源是恒等式`q=2p-2^k+1`

`q=Σ(2p_i-1)·2^i=2·Σp_i·2^i-Σ2^i=2p-(2^k-1)=2p-2^k+1`。

- `-1存0、1存1`→得到`p`；
- `<<1`→得到`2p`；
- 最低位填`1`→是那个`+1`（把每个-1数字欠下的-1汇总成`-2^k+1`补偿回来）；
- `p[k-1]`取反→是那个`-2^k`（把最高位权重从正翻成负）。

"取反与结果无关"的前提是**不溢出**：不溢出时`¬p[k-1]=p[k-2]`，最高位是符号扩展的冗余副本，最终丢弃，故取反与否不影响丢弃后的k位结果；溢出时该位不能丢，取反才必须。精确说取反的是`p[k-1]`（即`q[k-1]`对应的存储位），不是`q[k-1]`数字本身。例45/7：`p=(1 0 1 1)`→左移填1得`(1 0 1 1 1)`，丢最高位得`(0 1 1 1)=7`，与先取反`(0 0 1 1 1)`再丢最高位结果相同。

### 收尾校正

当`sign(s(k))≠sign(z)`时，商±1、余数∓`2^k·d`联合校正，把最后一下冲过头的部分补回来。

### 一句话硬件视角

判商=选操作（一次判断）；存商=存操作序列（-1当0、1当1）；转换=左移补1（最高位不溢出即丢弃）；最后按需校正。

## 2026.09.12 22:43

从5-bit完整计算推导出restoring的4-bit简化判商`q=msb(s) OR cout`。

### 推导链

把restoring的试探减法按5-bit（符号位+高4位）完整写出，结果的高位bit-5即符号位：

- `bit-5=1`→结果为负→不够减→`q=0`；`bit-5=0`→非负→足够减→`q=1`。故`q=~(bit-5的sum)`。

bit-5上全加器的三个输入：`a=msb(s)`（左移移出的那一位）、`b=1`（`-2^4d`为负、其最高位必为1）、`cin=cout`（低4位加法`A+~d+1`的进位输出）。故`sum(bit5)=msb(s)⊕1⊕cout`。

于是（用恒等式`a⊕1⊕b=¬(a⊕b)`即XNOR）：

```
q = ~sum = ~(msb(s) ⊕ 1 ⊕ cout) = msb(s) ⊕ cout
```

### 互斥性：XOR可降为OR

`msb(s)`与`cout`不可能同时为1：`msb(s)=1`意味着`s≥2^(2k-1)`，结合不变量`s<2^k·d`得`2s`的高k位`A=⌊s/2^(k-1)⌋ mod 2^k`落在`[0, 2d-2^k)`，而`d<2^k`给出`2d-2^k<d`，故`A<d`、`cout=0`。即`msb(s)=1`本身已把"足够减"的信息承接，低4位比较必显`cout=0`。因此XOR与OR重合：

```
q = msb(s) ⊕ cout = msb(s) OR cout
```

### 意义

该链从5-bit全加器出发（看符号位→符号位=bit5的sum→XNOR化简→互斥降OR），完整还原了4-bit简化版`q=msb(s) OR cout`：5-bit与4-bit是同一件事，多出的符号位正是`msb(s)`这个旗标，4-bit版本只是把它从加法器里拎出来单独处理。

## 2026.09.13 00:50

Divider.md统一符号表示与`sign(s)`/`msb(s)`区分的共识。

### 统一符号：`(位) N位`

每个数写成`(额外位) N位字段`，括号位是"因进位/移位/符号扩展产生的、有含义但不占寄存器空间的位"：

- 部分余数`s(j)=(符号) 8位`，是9-bit二补码；因`|s|<2^4·d≤160<256`恒成立，括号位就是**符号位**；
- `±2^4d=(符号) 4位`，是5-bit二补码。

### 关键修复：sign(s)≠8位字段的MSB

之前的错误是把**8位字段的MSB**当成符号。新表示里两者分开：

```
s(3) = (0) 10001000 = +136
        ↑符号位(0=正)  ↑8位字段MSB=1
```

`136`为正但8位字段`10001000`的MSB=1。符号是括号里的第9位，不是8位字段最高位。非恢复用`sign(s)`（括号位）决定加减，故`s(3)`的`sign=0`→做减法。

### 两个不同的"最高位"

| | 含义 | 用在 |
|---|---|---|
| `sign(s)` | 括号位（第9位），真正的符号 | Nonrestoring的加减决策 |
| `msb(s)` | 8位字段的MSB（移位时移出的那一位） | Restoring的`q=msb(s) XOR cout` |

- Restoring里`msb(s)=1`表示"s≥128、无论如何都够减"（s恒非负），故`q=msb(s) XOR cout`成立；
- Nonrestoring必须用`sign(s)`（括号位），因s可负、且正数136的8位MSB也是1。

### 三条轨迹核对（Divider.md重写后）

- Restoring 117/10：`117→74→148(还原)→136→112`，`q=1011`、`s=7`；
- Nonrestoring 117/10：`117→74→-12→136→112`，`sign(s)`驱动加减，`q=1011`、`s=7`；
- SRT 69/10：`69→-22→-44→-88→-16(校正)→144`，`q=6`、`s=9`。

### 修正的log

此前"non-restoring的5-bit对照推导"（`q=~(msb(s)⊕~msb(s)⊕cout)=cout`）因把`msb(s)`当成了符号而移除；restoring的`q=msb(s) OR cout`推导仍然正确（restoring中s恒非负、`msb(s)=1`确表"足够减"）。

## 2026.09.13 01:46

SRT引入冗余（0数字）所做的转换：把"与除数d比较"换成"与常数比较"，两个阈值数。

### 两个阈值

- 未规范化（非恢复"跳0"）：判断`2s`是否在`[-2^k·d, 2^k·d)`内 → 与**`2^k·d`**（除数对齐后的可变阈值）比较；
- 规范化后（SRT）：判断`2s`是否在`[-2^(2k-1), 2^(2k-1))`内 → 与**`2^(2k-1)`**（满量程一半的固定常数）比较。

### 为何能互换

`d ≥ 2^(k-1)` 给 出 `2^k·d ≥ 2^k·2^(k-1) = 2^(2k-1)`，即常数`2^(2k-1)`永不超过`2^k·d`，是保守的安全下界：跳0区间变小（跳得更保守、绝不误跳），但换来阈值成为常数、只需看最高两位。

### 一句话

SRT引入的冗余（商数字集`{-1,1}`扩成`{-1,0,1}`，多出"0"），把"部分余数是否够小、能否跳0"的判断，从与除数`d`（可变`2^k·d`）比较，转换成了与常数（满量程一半`2^(2k-1)`）比较。
