# agent-request

## 目标

- 目标是帮助我维护好Lumos LaTeX模板工程体系，并完成发布流程。
- 有些任务会要求你帮助测试和合并新的特性。
- 有些任务会要求你添加新的模板。
- 维护某个仓库时，形式上注重和同生态位的仓库保持一致，不要随意创造差异。

## 信息

- 这个项目具有特殊的复杂性：你面对的不是单一仓库，各个模板都有自己的独立仓库（当前仓库只是个主页）。
- 核心思想：文档模板`Class`、功能扩展`Package`、编译支持`Makefile`，这三者分别有独立仓库维护，本身不可直接使用。
- 核心思想：模板`Template`将上述三者以子模块形式引入，是直接可复用的版本。
- 通常在`dev`分支开发，发布前要同步到`master`分支上，许多更新是通过`feature/`和`fix/`的合入实现，它们往往来自下游应用（模板由我开发由我使用）。
- 比较特殊的是模板`Template`下的，目前`dev`用于测试各类特性，往往包含dummy内容，而`master`保持干净可直接使用的状态（未来可改革）。
- 需要支持两个源，`origin`指向个人服务器Lithium的私人仓库，`github`指向Github上的公开仓库。
- 比较特殊的是模板`Template`下的，它会需要一个特殊的`github`分支：因为Lithium和Github版本需要不同的子模块源。
- `Class`、`Package`、`Makefile`、`Template`这四个目录，和当前目录平级，你可以访问。
- `Plan.md`是过去的AI总结的文档写作规范。
- `autorepo`脚本用于自动在Lithium上创建远程仓库并添加远程引用，也可以用来添加Github的引用。

## 阶段目标

### VSP Beamer

- 仓库链接：https://github.com/Heaticy/vsp-beamer/tree/main
- 我希望可以将这个vsp beamer迁移到我的体系中。
- 我的目标：制作一个`Class`和一个`Template`，它的`Template`需要引入`MakefileLaTeX`的支持，但不用支持`Minimus`。
- 不需要迁移它和AI Skill相关的支持。
- 不需要迁移它的CI/CD机制。
- 文件名称可以适当做转写符合该项目的习惯，但命令定义不要动。
- 你可以克隆到临时目录，以观察它的结构，但是不要做任何对系统配置有影响的操作。
- 你需要调研清楚它的架构：提供了哪些主题和配置选项，有哪些特殊的机制。
- 你需要制定一个迁移方案，我们讨论后，执行。
